# Coding agent start prompt

You are implementing a fully client-side 11+ maths practice website. Use the files in this pack as the source of truth. Do not use or request the original extracted source questions.

Build around this model:

- Topics are broad tags.
- Skills are graph nodes and progress units.
- Archetypes are question-generator patterns under skills.

First implement content loading, ID validation, seeded RNG, a `QuestionInstance` type, and 10–20 text-only generators from the suggested first batch. Then implement skill practice with localStorage progress. Leave UI styling/design flexible and minimal.

For every generator, create original questions from the archetype spec. Do not copy any source wording. Use the synthetic examples only to understand the pattern.
